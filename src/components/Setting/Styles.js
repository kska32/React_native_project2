import { StyleSheet} from 'react-native';

export let uStyles = StyleSheet.create({
    border:{
        borderWidth: 1,
        borderColor: 'blue',
        borderStyle: 'solid',
        borderRadius: 5
    }
})
